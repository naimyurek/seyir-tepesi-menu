# Local dev tooling / editor cruft — not part of the published site
.claude/
.DS_Store
*.log

# Cloudflare Worker yerel önbelleği
.wrangler/

# DİKKAT: bot/README.md ve bot/wrangler.toml'a GERÇEK token/secret yazma!
# Gizli değerler yalnızca `wrangler secret put` ile Cloudflare'de durmalı.
